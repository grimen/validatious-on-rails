== TODO

== High

* validates_uniqueness_of/AJAX-bugs:
  * bug: Make it impossible to submit the form when this validation haven't passed

== Medium

* refactor: Validator#allow_blank => v2.Rails.js, and refactor the validators
* feature: Validator API: Custom validators (client-side + remote)
* feature: Form Input Helper API: Custom form helpers (non-"core helper" based)
* doc: Documentation of the above

== Low

* feature: Make Rails core validations basic options fully supported
  * :on => :create|:save|:update (not easy - RemoteValidator?)
  * :if/:unless => true|false (not easy - RemoteValidator?)
* test: Tighten up the tests more.

== Not sure

* feature: validates_association