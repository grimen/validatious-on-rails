== TODO

=== validatious-on-rails

* Respect validation macro options:
  * :on => :<action> (i.e. if current form action is ACTION)
  * :if/:unless => true|false
  * :allow_blank - using "acceptEmpty"
* Add client-side support for all the standard Rails validations (that are possible)
  * validates_format_of (Validator)
  * validates_exclusion_of (Validator)
  * validates_inclusion_of (Validator)
  * validates_uniqueness_of (RemoteValidator)
* Generate Validatious-compatible localized error messages, either using I18n-js lib, or own solution.
* Finalize custom client-side Validatious-validators (v2.Validator) with adapter-interface
* Server-side (custom) Validatious-validator support (v2.Validator)
* Support 3rd party form helpers through adapters?
* If needed (becuase how ValidationReflection is implemented - a bit hardcoded): Own validation reflection.
* Add README-note on how to make Formtastic and Validatious be nice to each other - just for the clarity

=== validatious

* Make the submit button be disabled (non-clickable) while the form contains invalid values (maybe)
